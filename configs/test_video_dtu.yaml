_parent_: configs/test_dtu.yaml

nerf:
    rand_rays_test: 20480                                         # number of random rays for each step
    render_video: true
    video_n_frames: 60
    video_rads_scale: 0.3
